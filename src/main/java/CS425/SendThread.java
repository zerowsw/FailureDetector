package CS425;

import java.util.logging.Logger;

public class SendingThread extends Thread {
    public static Logger logger = Logger.getLogger(SendingThread.class.getName());
    public  SendingThread(int port){

    }
}
